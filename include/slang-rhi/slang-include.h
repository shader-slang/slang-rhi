#pragma once

// Enable this to trigger a debug break whenever a SLANG_RETURN_ON_FAIL triggers
// #define SLANG_HANDLE_RESULT_FAIL(err) __debugbreak()

#include <slang-com-helper.h>
#include <slang-com-ptr.h>
#include <slang.h>
